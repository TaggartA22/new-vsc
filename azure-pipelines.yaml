trigger:
  - none
pr:
  - none

resources:
  repositories:
    - repository: 
      type: github
      name: TaggartA22/GitHub-Test
      ref: refs/heads/master
      endpoint: TaggartA22

parameters:
  - name: enviroment
    displayName: enviorment to deploy
    type: string
    default: sandbox
    values: 
      - dev
      - sandbox
      - test
      - prod
      
stages:
  - stage: stageTest
    displayName: stageTest
    jobs:
      - job: jobTest
        displayName: jobTest
        steps:
          - bash: |
              echo "This is testBranch"

